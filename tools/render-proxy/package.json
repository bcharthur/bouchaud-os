{
  "name": "nautile-render-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Service de rendu deporte pour le navigateur Nautile de Bouchaud OS : rend une URL avec un vrai Chromium headless et renvoie un PNG que l'OS affiche tel quel.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "sharp": "^0.33.5"
  }
}
