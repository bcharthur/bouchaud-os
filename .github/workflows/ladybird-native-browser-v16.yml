name: ladybird-native-browser-v16 (deprecated)

# V16 etait un clone du producteur canonique. Il est garde comme pointeur
# visible dans Actions afin que les anciens liens ne deviennent pas ambigus.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'V16 est maintenant integre a ladybird-native-browser.yml.'
          echo 'Lancer le workflow "ladybird-native-browser" a la place.'
